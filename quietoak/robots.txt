# Nested robots.txt is inert on this origin — only https://hypersage.ai/robots.txt is fetched
# by crawlers. This file is kept only so the URL is not a 404. Its Sitemap line is publicly
# fetchable, so it must not advertise a host we do not own: it previously pointed at
# https://quietoak.app/sitemap.xml, and quietoak.app is NXDOMAIN (README.md:31 open to-do).
User-agent: *
Allow: /

Sitemap: https://hypersage.ai/quietoak/sitemap.xml
