# hypersage.ai is live (2026-08-06) and this repo is attached to it, so this file
# is now at the ORIGIN ROOT and is actually in force. It was inert before: a crawler
# only ever reads robots.txt from an origin root, and the site was served from a
# project path, so the file that governed this host was drvanus.github.io/robots.txt.
#
# THIS IS THE ONLY robots.txt THAT DOES ANYTHING. Eight subfolders still carry one
# each (nightshelf/, storyvault/, mythwright/, wingmate/, saffra/, quietoak/,
# hexhunter/, mythkin/) — they arrived with the sites when they were folded in from
# their own repos, where each WAS an origin root. Nested robots.txt is ignored by
# every crawler, at every level, always, so they are inert rather than harmful; their
# stale Sitemap: lines are read by nobody. Delete them when convenient, and do not add
# more. The eleven product sites are subfolders of this one origin, so this file
# covers all of them.
User-agent: *
Allow: /

Sitemap: https://hypersage.ai/sitemap.xml
