# Crawlers only ever fetch /robots.txt from an origin ROOT. This site is served
# from a GitHub Pages *project* path (drvanus.github.io/waddleton-site/), so no
# crawler reads this file as things stand — the Sitemap line below only starts
# working if the site moves to its own domain. Real discovery today comes from
# the <link rel="sitemap"> tag in every page's <head>, not from here.
User-agent: *
Allow: /

Sitemap: https://drvanus.github.io/hypersage-site/waddleton/sitemap.xml
